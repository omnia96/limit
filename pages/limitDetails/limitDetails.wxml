<map 
    id="map4select" 
    longitude="{{longitude}}" 
    latitude="{{latitude}}" 
    markers="{{markers}}" 
    bindregionchange="regionchange" 
    polygons="{{polygon}}"
    style="width: 100%; height: 100vw;"
    scale="10"
    >
</map>